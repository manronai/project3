@model IEnumerable<project3.Models.employee>

@{
    ViewBag.Title = "show";
}

<h2>show</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.employeeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.employeeDetails)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.employeeRole)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.employeeSalary)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.employeeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.employeeDetails)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.employeeRole)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.employeeSalary)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.employeeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.employeeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.employeeID })
        </td>
    </tr>
}

</table>
